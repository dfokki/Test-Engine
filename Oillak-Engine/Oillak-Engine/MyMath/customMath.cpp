
#include "customMath.h"

	customMath::customMath()
	{
	}

	customMath::~customMath()
	{
	}

	//float customMath::Dot(const Vector2f &v1,const Vector2f &v2) 
	//{
	//	auto result = v1.x*v2.x  + v1.y+v2.y;
	//	return result;
	//}

	// customMath::normalize(const Vector2f &source)
	//{
	//	float length = sqrt((source.x * source.x) + (source.y * source.y));
	//	if (length != 0)
	//		return Vector2f(source.x / length, source.y / length);
	//	else
	//		return source;
	//}
